# Локальные настройки деплоя — скопировать в .env.deploy (файл в .gitignore)
# Пример значений, не реальный сервер

DEPLOY_HOST=203.0.113.10
DEPLOY_PORT=22
DEPLOY_USER=root
DEPLOY_DIR=/opt/max-moderator-bot
DEPLOY_DOMAIN=bot.example.com
DEPLOY_WEB_PORT=3010
DEPLOY_SSH_IDENTITY=~/.ssh/id_ed25519

# Hestia CP (опционально; если не используете — оставьте пустым)
# HESTIA_USER=hestiauser
# DEPLOY_BIND_IP=203.0.113.10
# DEPLOY_NGINX_OLD_PROXY=http://203.0.113.10:8080
